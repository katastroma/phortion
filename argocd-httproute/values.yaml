name: argocd-httproute

global:
  domain:
    internal: localhost
    external: ''

gateway_system:
  name: default
  namespace: gateway-system

ingress:
  subdomain: argocd

argocd:
  ref:
    name: argocd-server
    port: 80
