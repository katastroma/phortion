apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  namespace: {{ .Values.global.gateway.namespace }}
  name: {{ .Values.global.gateway.name }}
spec:
  gatewayClassName: {{ .Values.gateway.className }}
  listeners:
    {{- if .Values.global.domain.internal }}
    - name: internal
      port: {{ .Values.ports.websecure }}
      protocol: HTTPS
      hostname: "*.{{ .Values.global.domain.internal }}"
      allowedRoutes:
        namespaces:
          from: All
      tls:
        mode: Terminate
        certificateRefs:
          - group: ''
            kind: Secret
            name: {{ .Values.global.domain.internal }}-tls
    {{- end }}
    {{- if .Values.global.domain.external }}
    - name: external
      port: {{ .Values.ports.websecure }}
      protocol: HTTPS
      hostname: "*.{{ .Values.global.domain.external }}"
      allowedRoutes:
        namespaces:
          from: All
      tls:
        mode: Terminate
        certificateRefs:
          - group: ''
            kind: Secret
            name: {{ .Values.global.domain.external }}-tls
    {{- end }}
