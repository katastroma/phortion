---
apiVersion: v1
kind: ServiceAccount
metadata:
  namespace: {{ .Values.global.namespace }}
  name: {{ .Values.stolarches.name }}
  labels:
    app.kubernetes.io/managed-by: katastroma
    app.kubernetes.io/part-of: katastroma
---
apiVersion: v1
kind: Service
metadata:
  namespace: {{ .Values.global.namespace }}
  name: {{ .Values.stolarches.name }}
  labels:
    app.kubernetes.io/managed-by: katastroma
    app.kubernetes.io/part-of: katastroma
spec:
  selector:
    app.kubernetes.io/name: {{ .Values.stolarches.name }}
  ports:
    - name: grpc
      protocol: TCP
      port: 50051
      targetPort: 50051
---
apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: {{ .Values.global.namespace }}
  name: {{ .Values.stolarches.name }}
  labels:
    app.kubernetes.io/name: {{ .Values.stolarches.name }}
    app.kubernetes.io/managed-by: katastroma
    app.kubernetes.io/part-of: katastroma
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ .Values.stolarches.name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ .Values.stolarches.name }}
    spec:
      serviceAccountName: {{ .Values.stolarches.name }}
      containers:
        - name: orderer
          image: {{ .Values.stolarches.image.repository }}:{{ .Values.stolarches.image.tag }}
          ports:
            - name: grpc
              containerPort: 50051
              protocol: TCP
          env:
            - name: OTEL_TRACES_EXPORTER
              value: otlp
            - name: OTEL_METRICS_EXPORTER
              value: otlp
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: prymna-opentelemetry-collector:4317
